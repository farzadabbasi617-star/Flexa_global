# ─────────────────────────────────────────────────────────────
#  gament — environment variables
#  Copy this file to ".env" and fill in real values.
#  NEVER commit the real ".env" file (it is git-ignored).
# ─────────────────────────────────────────────────────────────

# PostgreSQL connection string (e.g. from Neon).
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=verify-full
DATABASE_URL="postgresql://user:password@host/database?sslmode=verify-full"

# OpenRouter API Key
OPENROUTER_API_KEY="your_openrouter_api_key"

# Groq API Key
GROQ_API_KEY="your_groq_api_key"

# HuggingFace API Key (Inference Router, OpenAI-compatible) — used as the 3rd
# AI failover provider after OpenRouter and Groq.
HUGGINGFACE_API_KEY="your_huggingface_api_key"
# Optional: override the first HuggingFace model to try.
# HUGGINGFACE_MODEL="meta-llama/Llama-3.3-70B-Instruct"

# Recommended on Render Free: Gmail via Google Apps Script HTTPS relay.
# See docs/GOOGLE_APPS_SCRIPT_EMAIL.md. Never commit/share the secret.
EMAIL_PROVIDER="google_apps_script"
GOOGLE_APPS_SCRIPT_EMAIL_URL="https://script.google.com/macros/s/DEPLOYMENT_ID/exec"
GOOGLE_APPS_SCRIPT_EMAIL_SECRET="your_long_random_shared_secret"

# Optional on paid hosts where SMTP ports are open.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="gament1.ir@gmail.com"
SMTP_PASS="your_16_character_gmail_app_password"
SMTP_FROM_EMAIL="Gament <gament1.ir@gmail.com>"

# Optional alternative provider: Resend. Set EMAIL_PROVIDER="resend" to use it.
RESEND_API_KEY="your_resend_api_key"
RESEND_FROM_EMAIL="Gament <noreply@gament1.ir>"

# FarazSMS API Configuration (optional; not used by the current auth flow)
# Kept for future SMS features (e.g. match reminders); registration/login no
# longer requires it since verification codes are emailed instead.
FARAZSMS_API_KEY="your_api_key_from_farazsms"
FARAZSMS_PATTERN_CODE="your_pattern_code"
FARAZSMS_SENDER="+983000505"

# Clash Royale API. On Render Free, the temporary RoyaleAPI proxy key must be
# created for proxy IP 45.79.218.79. Never commit or share the token.
CLASH_ROYALE_API_TOKEN="your_supercell_api_token"
CLASH_ROYALE_API_BASE_URL="https://proxy.royaleapi.dev/v1"

# Telegram bot integration
# Recommended free mode: Telegram webhook runs inside this Next.js web service.
BOT_TOKEN="telegram_bot_token_from_botfather"
TELEGRAM_WEBHOOK_SECRET="change_this_to_another_long_random_secret"
TELEGRAM_ADMIN_IDS="123456789"
TELEGRAM_CHANNEL_URL="https://t.me/Flexa_games"
TELEGRAM_CHANNEL_ID="@Flexa_games"
TELEGRAM_BOT_USERNAME="FlexaTournamentBot"
TELEGRAM_CRON_SECRET="change_this_if_you_use_cron_job_org"
# Separate secret for POST /api/telegram/setup (never put it in a public URL).
TELEGRAM_SETUP_SECRET="change_this_to_a_different_long_random_secret"
TELEGRAM_REQUIRE_CHANNEL_MEMBERSHIP="true"
# Set true only if every Telegram flow must require an existing Gament account.
TELEGRAM_GAMENT_ID_REQUIRED="false"
# Mini App initData is accepted for one hour by default. Range: 60..86400 seconds.
TELEGRAM_INIT_DATA_TTL_SECONDS="3600"
# Optional trusted public Telegram news channels. Format: game:channel_username
# Example: clash_royale:OfficialChannel,cod_mobile:AnotherTrustedChannel
GAMING_NEWS_TELEGRAM_CHANNELS=""
# Maximum new trusted articles per five-minute cron run (not a daily limit).
GAMING_NEWS_MAX_PER_RUN="4"

# Media affiliate program starts in shadow mode. Set true only after reviewing
# at least 30 days of calculated commissions and partner fraud signals.
AFFILIATE_PROGRAM_LIVE="false"
# Real accrual requires all three approvals plus a rollout mode. Canary mode
# only creates real Pending events for referred users in the Gament-ID allowlist.
AFFILIATE_LEGAL_APPROVED="false"
AFFILIATE_FINANCE_APPROVED="false"
AFFILIATE_LIVE_ROLLOUT="canary"
AFFILIATE_CANARY_GAMENT_IDS=""
# COD Arena remains private-beta/shadow until room operations, legal review,
# evidence handling and two QA financial cycles have been approved.
COD_ARENA_LIVE="false"
COD_ARENA_FINANCE_APPROVED="false"
AFFILIATE_ATTRIBUTION_DAYS="30"
AFFILIATE_COMMISSION_TOMAN="7000"
AFFILIATE_HOLD_HOURS="72"
AFFILIATE_MINIMUM_PAYOUT_TOMAN="300000"
PERSONAL_REFERRAL_MINIMUM_PAYOUT_TOMAN="200000"
# Optional legacy/server-to-server integration for the Python worker bot.
TELEGRAM_INTEGRATION_SECRET="change_this_to_a_long_random_secret"
# Required in production before calling POST /api/admin/setup to bootstrap the first super admin.
ADMIN_SETUP_SECRET="change_this_to_a_long_random_secret"
# Optional but recommended before enabling SMS OTP; used as a pepper for OTP hashes.
OTP_TOKEN_PEPPER="change_this_to_a_different_long_random_secret"

# Optional: secret for internal AI/automation endpoints that suggest Hall of Fame items.
HONORS_AI_SECRET="change_this_to_a_long_random_secret"

# Optional: Cloudinary upload for admin media manager
# If not set, small images can still be stored as data URLs.
CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"

# Optional: enable hourly auto-scan of Divar/Sheypoor for gaming ads.
# Set to "true" only after you have applied the classified_ads migration.
CLASSIFIED_AUTO_SCAN_ENABLED="false"
# How often to scan (in hours). Default is 1.
CLASSIFIED_SCAN_INTERVAL_HOURS="1"
# Scan all cities (true) or only Tehran (false).
CLASSIFIED_SCAN_ALL_CITIES="false"
# Max ads per city per scan. Keep low to avoid IP/rate-limit bans.
CLASSIFIED_LIMIT_PER_CITY="5"
# Optional HTTP proxy for classified scraping AND the AI price-estimator's live
# market lookups. Divar/Sheypoor geo-block non-Iran servers, so if your app is
# hosted abroad (e.g. Render) live scraping returns nothing and the estimator
# relies on the AI model's market knowledge. Point this at an Iran-based fetch
# proxy that takes a ?url= param to enable real live comparables.
# Example: https://your-iran-proxy.example/fetch
CLASSIFIED_PROXY_URL=""
# Optional: comma-separated keywords. If set, only ads containing at least one are kept.
CLASSIFIED_WHITELIST_KEYWORDS=""
# Optional: comma-separated keywords. Ads containing any of these are skipped.
CLASSIFIED_BLACKLIST_KEYWORDS=""
# Delete classified ads older than N days. Default 7, set 0 to disable.
CLASSIFIED_ADS_MAX_AGE_DAYS="7"

# Node environment: development | production
NODE_ENV="development"

# Runtime schema bootstrap (DDL from inside request handlers).
# Off in production by default: the schema comes from reviewed migrations via
# ./scripts/apply-migrations.sh. Setting this to "true" restores the legacy
# behaviour where COD/wallet services run CREATE/ALTER TABLE on the request
# path — useful for a scratch dev database, risky on a live wallet deploy.
# ALLOW_RUNTIME_SCHEMA_DDL="false"

# ─────────────────────────────────────────────────────────────
# ZarinPal payment gateway (online wallet top-up)
# ─────────────────────────────────────────────────────────────
# The 36-character UUID from the ZarinPal panel. This is NOT the ZP.xxxxxxx code
# shown on a ZarinLink page -- a ZarinLink is a personal payment page: it has no
# callback, is not tied to a user or order, and cannot credit a wallet.
ZARINPAL_MERCHANT_ID=""

# Master switch. A valid merchant id alone never charges a card; this must also
# be "true". Same pattern as COD_ARENA_LIVE / AFFILIATE_PROGRAM_LIVE.
ZARINPAL_LIVE="false"

# Route requests to ZarinPal's sandbox instead of production.
ZARINPAL_SANDBOX="false"

# Public https origin used to build the gateway callback URL. Must match the
# domain registered with ZarinPal; http:// is rejected by our own config check.
PAYMENT_CALLBACK_BASE_URL="https://www.gament1.ir"
